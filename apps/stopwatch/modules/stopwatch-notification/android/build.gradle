plugins {
  id 'com.android.library'
  id 'expo-module-gradle-plugin'
}

group = 'org.splitsync.stopwatch.notification'
version = '1.0.0'

android {
  namespace "org.splitsync.stopwatch.notification"
  defaultConfig {
    versionCode 1
    versionName "1.0.0"
  }
  lintOptions {
    abortOnError false
  }
}
